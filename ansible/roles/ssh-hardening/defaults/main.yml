---
# ============================================================
# Ansible Role: ssh-hardening — Default Variables
# Based on Mozilla OpenSSH Security Guide + BSI TR-02102-4
# ============================================================

# Key Exchange Algorithms (only modern, PFS — single line required by sshd)
ssh_kex_algorithms: "curve25519-sha256,curve25519-sha256@libssh.org,ecdh-sha2-nistp521,ecdh-sha2-nistp384,ecdh-sha2-nistp256,diffie-hellman-group-exchange-sha256"

# Symmetric ciphers (AEAD preferred — single line required by sshd)
ssh_ciphers: "chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com,aes256-ctr,aes192-ctr,aes128-ctr"

# Message Authentication Codes (ETM required — single line required by sshd)
ssh_macs: "hmac-sha2-512-etm@openssh.com,hmac-sha2-256-etm@openssh.com,umac-128-etm@openssh.com,hmac-sha2-512,hmac-sha2-256"

# Connection limits
ssh_max_auth_tries: 3
ssh_login_grace_time: 30
ssh_max_sessions: 5
ssh_client_alive_interval: 300
ssh_client_alive_count_max: 2

# Access controls
ssh_permit_root_login: "no"
ssh_x11_forwarding: "no"
ssh_allow_tcp_forwarding: "no"
ssh_allow_agent_forwarding: "no"
ssh_permit_tunnel: "no"
ssh_allow_users: "azureadmin"

# Authentication
# NOTE: PasswordAuthentication ramas "yes" intentionat.
# Hardening-ul se concentreaza pe algoritmi/ciphers, nu pe blocarea parolelor.
ssh_pubkey_authentication: "yes"
ssh_password_authentication: "yes"
ssh_challenge_response_authentication: "no"

# Logging
ssh_log_level: "VERBOSE"
ssh_syslog_facility: "AUTH"

# Banner
ssh_banner: /etc/ssh/banner.txt
ssh_banner_text: |
  ******************************************************************
  *  SC MEDIA SRL — Authorized Access Only                        *
  *  All connections are monitored and logged.                     *
  *  Unauthorized access is prohibited and will be prosecuted.     *
  ******************************************************************
