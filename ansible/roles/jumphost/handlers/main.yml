# ============================================================
# Ansible Role: jumphost - Handlers
# ============================================================

- name: restart xrdp
  ansible.builtin.systemd:
    name: xrdp
    state: restarted
