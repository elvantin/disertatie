---
# ============================================================
# Ansible Role: jumphost - Handlers
# ============================================================

- name: restart xrdp
  ansible.builtin.systemd:
    name: xrdp
    state: restarted

- name: restart ssh
  ansible.builtin.systemd:
    name: ssh
    state: restarted
