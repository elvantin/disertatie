---
# ============================================================
# Group Variables for Jumphost (vm-jmp-01)
# Overrides linux group vars for local connection
# ============================================================

# Jumphost runs playbooks locally, no SSH needed
ansible_connection: local
ansible_become: no
